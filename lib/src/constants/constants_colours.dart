import 'package:flutter/material.dart';

class ConstantColours {
  static const white = Colors.white;
  static const black = Colors.black;
  static const grey = Color.fromRGBO(243, 243, 243, 1.0);
  static const secondary = Color.fromRGBO(38, 13, 70, 1.0);
  static const secondaryNewGrad = Color.fromRGBO(62, 6, 113, 1.0);
  // static const primaryColor = Color.fromRGBO(254, 204, 9, 1.0);
  static const primaryNew = Color.fromRGBO(254, 204, 9, 1.0);
}
